{
  "build": {
    "env": {
      "NODE_VERSION": "18"
    },
    "command": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve dist",
    "healthcheckPath": "/",
    "restartPolicy": "on-failure",
    "maxRetries": 10
  }
}
